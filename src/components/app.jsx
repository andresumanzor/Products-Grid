import React from 'react';
import TopBar from './topBar';
import Products from './products/products';

export default 
    ({}) => {
  
    return (
        <React.Fragment>
          <TopBar/>
          <Products/>
        </React.Fragment>
    );
};