import React from 'react';
import TopBar from './topBar';

export default 
    ({}) => {
  
    return (
        <React.Fragment>
          <TopBar/>
          Try #1 with all the basics set up!
        </React.Fragment>
    );
};